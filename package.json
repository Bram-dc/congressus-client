{
	"name": "@itbg-dsc/congressus-client",
	"version": "1.0.0",
	"private": false,
	"type": "module",
	"exports": {
		".": "./dist/src/index.js"
	},
	"files": [
		"dist",
		"src",
		"README.md"
	],
	"scripts": {
		"lint": "biome check --write",
		"client:generate": "openapi-ts && npm run lint",
		"build": "rimraf dist && tsc",
		"prepare": "npm run build",
		"prepublishOnly": "npm run build"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.3",
		"@hey-api/openapi-ts": "^0.82.4",
		"rimraf": "^6.0.1"
	}
}
